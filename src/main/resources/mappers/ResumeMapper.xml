<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.resume_management.resume_management_back.resume.dao.ResumeMapper">

    <select id="findAllRow"
            resultType="com.resume_management.resume_management_back.resume.domain.ResumeResponseDTO">
        SELECT user_id, email, name, skill, profile_image
        FROM user_tbl;
    </select>

</mapper>